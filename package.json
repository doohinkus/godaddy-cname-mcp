{
  "type": "module",
  "name": "godaddy-dns-mcp",
  "version": "1.0.0",
  "description": "Production-grade MCP server for managing GoDaddy DNS records via the Model Context Protocol",
  "main": "dist/index.js",
  "bin": {
    "godaddy-dns-mcp": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "axios": "^1.6.0",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
